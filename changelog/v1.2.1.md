# v1.2.1 — 2026-02-21

## Fixed

### ESLint Errors Across Test Suite (36 errors → 0)

All pre-existing ESLint errors in the test suite have been resolved so that
`npm run lint` exits cleanly and the CI pipeline passes without failures.

#### `tests/scripts/analyze-components.test.ts`
- Added `eslint-disable-next-line no-control-regex` above ANSI regex assertion
- Removed unused `let testComponentB` declaration from describe block

#### `tests/scripts/analyze-deps.test.ts`
- Added `eslint-disable-next-line no-control-regex` above ANSI regex assertion
- Removed unused imports: `hasDependency`, `addImportToFile`, `path`, `fs`

#### `tests/scripts/analyze-pages.test.ts`
- Added `eslint-disable-next-line no-control-regex` above ANSI regex assertion
- Removed unused import `getPageComponentPath`
- Removed unused `let testPageName` from `page analysis accuracy` describe block
- Removed unused `let testSubComponentName` / `testSubComponentPath` scaffolding
  from `integration with test fixtures` describe block

#### `tests/scripts/create-page.test.ts`
- Removed unused import `viteConfigHasPage`

#### `tests/scripts/delete-block.test.ts`
- Removed unused imports: `cleanupTestArtifacts`, `getComponentTestPath`
- Added `expect(result1).toBeDefined()` and boolean-type assertion for
  `block1StillExists` in the dependency-ordering test

#### `tests/scripts/delete-component.test.ts`
- Removed unused import `cleanupTestArtifacts`
- Added `expect(result1).toBeDefined()` in the dependency-ordering test

#### `tests/scripts/delete-page.test.ts`
- Removed unused imports: `cleanupTestArtifacts`, `PATHS`
- Changed `catch (e)` → `catch` (optional binding, TypeScript 4.0+)

#### `tests/scripts/delete-ui-component.test.ts`
- Removed unused import `cleanupTestArtifacts`
- Added `expect(result1).toBeDefined()` in the dependency-ordering test

#### `tests/scripts/helpers/test-utils.ts`
- Changed all `catch (e)` → `catch` for intentionally-ignored error variables

#### `src/hooks/useSectionNavigation.ts`
- Removed now-redundant `eslint-disable-next-line react-hooks/exhaustive-deps`
  directive (deps array is correct; ESLint 9 no longer flags it)
